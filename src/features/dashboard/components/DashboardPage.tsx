import { Box, Typography } from '@mui/material';

/**
 * Placeholder de la Fase 1.
 * El Dashboard con datos mock se construye en la Fase 4.
 */
export function DashboardPage() {
  return (
    <Box sx={{ p: 4 }}>
      <Typography variant="h4">Dashboard — pendiente (Fase 4)</Typography>
    </Box>
  );
}
