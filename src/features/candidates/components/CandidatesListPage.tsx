import { Box, Typography } from '@mui/material';

/**
 * Placeholder de la Fase 1.
 * El listado y detalle de candidatos se construyen en la Fase 5.
 */
export function CandidatesListPage() {
  return (
    <Box sx={{ p: 4 }}>
      <Typography variant="h4">Candidatos — pendiente (Fase 5)</Typography>
    </Box>
  );
}
